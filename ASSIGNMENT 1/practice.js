// Mini Project

let budget = 5000
let itemPrice = 1200

console.log( budget > itemPrice )  && ( budget > 0 )

//The item is affordable as it is lesser than the itemPrice and the budget is greater than 0.